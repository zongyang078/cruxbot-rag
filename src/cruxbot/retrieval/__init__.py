"""Retrieval stack: dense, sparse, and their fusion.

Modules here import heavy dependencies (sentence-transformers, chromadb) and
are therefore not imported at package load time. Import the submodule you need.
"""
